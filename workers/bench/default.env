WORKER_NAME=bench-api-local
# Dev placeholder — the real App id lives in the cd-worker-bench environment; the relay only
# needs it when actually minting tokens against GitHub.
BENCH_GH_APP_ID=0
# Serve Swagger UI (/v1/docs) + OpenAPI (/v1/openapi.json) locally. Never set in production.
DOCS_ENABLED=true
