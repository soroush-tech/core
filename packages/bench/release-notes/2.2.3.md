## @soroush.tech/bench@2.2.3

Maintenance release. Two reporter strings change; no API changes.

### Changed

- The progress line written to stderr while a round runs ends in three periods rather than an
  ellipsis character: `round 2/5 running...`.
- A table cell with no value for the measurement prints `-` rather than an em dash.

Anything asserting on that exact stderr line or on the em dash in a report cell needs updating.
Nothing about the measurements, the gate, or the JSON output changed.
