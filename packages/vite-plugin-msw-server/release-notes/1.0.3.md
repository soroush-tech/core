## @soroush.tech/vite-plugin-msw-server@1.0.3

Maintenance release — dependency refresh only. No public API or behavior changes.
([#280](https://github.com/soroush-tech/core/pull/280))

### Packaging

- Dev-dependency bumps: `eslint` `^10.7.0` → `^10.8.0`, `globals` `^17.7.0` → `^17.9.0`,
  `tsdown` `^0.22.7` → `^0.22.14`, `vite` `^8.1.4` → `^8.2.0`. The `vite` peer range stays
  `^6 || ^7 || ^8` — only the version built and tested against moved. No runtime dependency
  changes.
