/**
 * Gists shown per page in the panel. Independent of how many main asks GitHub
 * for at a time — the whole list is already loaded, so this is purely how much
 * of it the rail shows at once.
 */
export const GISTS_PER_PAGE = 10
