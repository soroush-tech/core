/**
 * What the document is called in a sandbox nobody has named yet. A gist file
 * needs a name before it can be staged, and one that can be renamed later is
 * better than refusing to save until something is typed into a filename box.
 */
export const DEFAULT_FILE = 'en.md'
