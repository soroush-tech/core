import type { Config } from 'vike/types'

export default {
  title: '@soroush.tech/bench',
  description:
    'Run A/B function and package-version benchmarks inside a CPU/RAM-pinned Docker sandbox for stable, low-noise numbers. Built on mitata.',
} satisfies Config
