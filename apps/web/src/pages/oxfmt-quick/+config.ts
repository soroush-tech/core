import type { Config } from 'vike/types'

export default {
  title: 'oxfmt-quick',
  description:
    'Run oxfmt on your changed files, and re-stage them. A pre-commit formatter for the oxc toolchain — what pretty-quick is to Prettier.',
} satisfies Config
