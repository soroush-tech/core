import { Layout } from 'src/common/Layout'
import { PackageIndex } from 'src/section/PackageIndex'

function NpmPage() {
  return (
    <Layout>
      <PackageIndex />
    </Layout>
  )
}

export default NpmPage
